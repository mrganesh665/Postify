const PageLoader = () => (
  <div className="fixed inset-0 bg-black/40 flex items-center justify-center z-50">
    <div className="w-50 h-50 border-4 border-white border-t-transparent rounded-full animate-spin" />
  </div>
);

export default PageLoader;
